[project]
name = "must-gather-local-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.7.6",
    "langchain>=1.3.15",
    "langchain-core>=1.5.6",
    "langchain-openai>=1.5.1",
    "langgraph>=1.2.11",
    "ollama>=0.6.2",
    "typing-extensions>=4.16.0",
]
